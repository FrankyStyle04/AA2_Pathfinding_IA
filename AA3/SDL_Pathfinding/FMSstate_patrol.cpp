#include "FMSstate_patrol.h"

FSMstate* FMSstate_patrol::Update(Agent* agent, float dtime)
{
    //Agent deploy state actions/movement

    //Transitions between states are checked here

    // if(time_to_change) return newState;
    // else return nullptr;
}

